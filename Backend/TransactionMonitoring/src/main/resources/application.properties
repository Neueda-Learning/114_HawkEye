spring.application.name=TransactionMonitoring
server.port=8080

# ===== DATASOURCE =====
# Keep local dev pinned to the project schema DB; legacy DB_URL env values can break create-transaction flows.
spring.datasource.url=jdbc:mysql://localhost:3306/txn?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=n3u3da!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA / HIBERNATE =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===== JACKSON =====
spring.jackson.time-zone=UTC

# ===== ACTUATOR =====
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when_authorized

# ===== EMAIL / NOTIFICATIONS =====
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:hawkeyee2026@gmail.com}
spring.mail.password=${MAIL_PASSWORD:isluwfnxobbyfupv}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_SMTP_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${MAIL_SMTP_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_SMTP_TIMEOUT:5000}

# Email notification settings - enabled by default for proper event listening
notification.email.enabled=${NOTIFICATION_EMAIL_ENABLED:true}
notification.email.from=${NOTIFICATION_EMAIL_FROM:hawkeyee2026@gmail.com}
notification.email.to=${NOTIFICATION_EMAIL_TO:akshayanetha0205@gmail.com}
notification.email.transaction-recipients=${NOTIFICATION_TX_RECIPIENTS:akshayanetha0205@gmail.com}
notification.email.alert-recipients=${NOTIFICATION_ALERT_RECIPIENTS:akshayanetha0205@gmail.com}
notification.email.rule-recipients=${NOTIFICATION_RULE_RECIPIENTS:akshayanetha0205@gmail.com}

# ===== EVENT PROCESSING =====
# Enable transactional event listeners
spring.events.async.support-enabled=true
